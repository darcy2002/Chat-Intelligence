# Chat Intelligence

AI chat with per-message intent and sentiment classification.

## Quick start

Backend:
```bash
cd backend
npm install
npm run dev
```

Frontend (separate terminal):
```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173.

Phase 1: skeleton with hardcoded backend response. LLM providers added in later phases.
